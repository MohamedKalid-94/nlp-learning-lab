# This project doesn't need any API keys for the core pipeline
# (everything runs locally — spaCy, NLTK, scikit-learn)
# Placeholder in case you extend it later with an LLM call
GROQ_API_KEY=your_key_here